apply plugin: 'com.android.application'

android {
    namespace 'com.example.blecall'
    compileSdk 34

    defaultConfig {
        applicationId "com.example.blecall"
        minSdk 26
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        debug {
            debuggable true
        }
    }
}

repositories {
    google()
    mavenCentral()
}

dependencies {
}